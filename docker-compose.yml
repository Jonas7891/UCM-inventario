version: '3.8'

services:
  inventario-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ucm-inventario-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
    volumes:
      - ./data:/app/data
